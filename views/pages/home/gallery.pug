- var data = dataGlobal.home.gallery;

mixin img(src)
	figure.gallery__item 
		img.gallery__image(data-src=src width="316"  height="316")

.gallery 	
	.gallery__wrapper 
		each src in data.images
			+img(src)
